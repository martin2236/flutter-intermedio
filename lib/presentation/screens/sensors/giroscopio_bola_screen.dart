import 'package:flutter/material.dart';

class GiroscopioBolaScreen extends StatelessWidget {
  const GiroscopioBolaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('giroscopio bola screen'),
      ),
      body: Center(
        child: Text('hola'),
      ),
    );
  }
}